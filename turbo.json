{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["BOT_TOKEN", "CLIENT_ID"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}

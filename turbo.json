{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "BOT_TOKEN", 
    "CLIENT_ID",
    "NODE_ENV"
  ],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "server#build": {
      "dependsOn": ["^build"],
      "env": ["DROP_SCHEMA"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
